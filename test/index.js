import {
  setup,
  teardown
} from './utils/db'

describe('netiam', () => {
  describe('REST - state', () => {

    before(setup)
    after(teardown)

    it('should add state', () => {
    })

  })
})
